export default function DashboardNavbar({ user }: { user: any }) {
  return (
    <div>
      <h1>qwebqwb</h1>
      {/* {session?.user.name}
      <Button onClick={onSubmit}>Sign out</Button> */}
    </div>
  );
}
